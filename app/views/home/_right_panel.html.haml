.right-panel
  You have
  = @jcb_left_this_month
  JCB to use this month.

.right-panel
  %a{:href => new_reputation_path, style: 'text-decoration: none;'}
    %button{class: 'btn btn-success btn-block'}
      Want give a JCB to someone?

.right-panel
  - @users.each do |user|
    %p
      = user.name
      = user.reputations.last_month.length
